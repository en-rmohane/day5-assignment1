#!/bin/bash -x

a=10
b=20
c=30

if [ $a -gt $b ] && [ $a -gt $c ]
then
    echo "a is the greatest number"
elif [ $b -gt $c ]
then
    echo "b is the greatest number"
else
    echo "c is the greatest number"
fi

